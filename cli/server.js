// local server for development

module.exports = function server(port) {
  // TODO: Implement the server command
  console.log(`Server is running on port ${port}`)

  // host bento server

  // host user project
}