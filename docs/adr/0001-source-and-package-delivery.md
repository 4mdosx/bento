# ADR 0001：源码交付与包交付

## 决策

Primitive、Part、View 和 Layout 由 Registry/CLI 写入应用源码；Host、Core、Adapter 和 UI Runtime 通过版本化包交付。

## 理由

视觉代码需要可读、可改，运行状态、副作用和跨端行为需要可升级的一致边界。源码层可以依赖包层的公共入口，包层不得反向依赖源码层。

## 后果

源码文件允许用户修改，升级时必须显示差异并尊重本地改动。包 API 遵守语义化版本，并提供类型、默认行为和逃生口。
